#include <iostream>

#include "myjson.h"

int main() {
    // myjson::json j1;
    // default Null class

    // myjson::json j1 = myjson::make_json(str);
    // stanrdard json constructor

    // function myjson::json j1 = myjson::parse(str);
    // parse json string to json object

    // j1.get_type();
    // return std::string of the object j type

    // j1.get_data();
    // return std::variant of the object j data

    // j1.get<type>();
    // return the data of the object j, if type is right,
    // else throw error

    // j1.get_to<type>(other);
    // assign j1 to other if type is right for the type of j1 and other
    // else throw error

    // j1.to_array<type>()
    // return std::vector<type> of the object j data if type is right

    // j1.to_map<type>()
    // return std::map<std::string, type> of the object j data if type is right
}